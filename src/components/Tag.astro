---
interface Props {
	class?: string;
}

const { class: className = '' } = Astro.props;
---

<span class={`rounded border border-surface bg-background px-3 py-1 font-code text-xs text-muted ${className}`}>
	<slot />
</span>
